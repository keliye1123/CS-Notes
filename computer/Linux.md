# Linux
 
- [Linux](#linux)  
  - [一、基础知识](#一基础知识)  
    - [路径描述](#路径描述)
    - [相对路径与绝对路径](#相对路径与绝对路径)
    - [特殊路径符](#特殊路径符)
    - [快捷键](#快捷键)
    - [通配符](#通配符)
  - [二、基础命令](#二基础命令)
    - [ls命令](#ls命令)
    - [cd命令：切换工作目录](#cd命令切换工作目录)
    - [pwd命令](#pwd命令)
    - [mkdir命令：创建新的目录](#mkdir命令创建新的目录)
    - [touch命令：创建文件](#touch命令创建文件)
    - [cat命令：查看文件内容](#cat命令查看文件内容)
    - [more命令：查看文件内容](#more命令查看文件内容)
    - [cp命令：复制文件文件夹](#cp命令复制文件文件夹)
    - [mv命令：移动文件或文件夹](#mv命令移动文件或文件夹)
    - [rm命令：删除文件、文件夹](#rm命令删除文件文件夹)
    - [find命令-：按文件名查找文件和按文件大小查找文件](#find命令-按文件名查找文件和按文件大小查找文件)
    - [管道符 |](#管道符)
    - [grep命令](#grep命令)
    - [wc命令做数量统计](#wc命令做数量统计)
    - [echo命令](#echo命令)
    - [重定向符](#重定向符)  
  - [三、vim编辑器](#vim编辑器)
    - [打开vim编辑器](#打开vim编辑器)
    - [vim编辑器的三种工作方式](#vim编辑器的三种工作方式)
  - [四、进程管理](#进程管理)
## 一、基础知识
  
### 路径描述
- 在Linux系统中，路径之间的层级关系，使用：/来表示。
- 在Windows系统中，路径之间的层级关系，使用：\来表示。
- Linux只有一个顶级目录，称之为：根目录，而windows系统一般有两个顶级目录分别是C盘和D盘。
- Linux系统中要从根目录开始描述路径必须以/开头如/home表示在根目录下的home文件夹。  
  
### 相对路径与绝对路径
- 绝对路径：以根目录为起点，描述路径的一种写法，路径描述以/开头。
- 相对路径：以当前目录为起点，描述路径的一种写法，路径描述无需以/开头。
  
### 特殊路径符
- . 表示当前目录，比如./Desktop 表示当前目录下的Desktop目录。
- .. 表示上一级目录，比如：../home表示上一级目录下的home文件夹，../../sys表示上上级目录下的sys文件夹。
- ~ 表示home目录，Linux系统都会有一个固定的名为home的文件夹。   
  
### 快捷键
- ctrl + c:终止当前窗口运行程序停止输出，命令输入错误可以退出当前命令输入。
- ctrl + d:退出某些特定程序的专属页面（不能退出vim）。
- ctrl + l:清屏，等效于clear命令。
  
### 通配符
- 符号*表示通配符，即匹配任意内容（包含空），示例：
- test*，表示匹配任何以test开头的内容。
- *test，表示匹配任何以test结尾的内容。
- *test*，表示匹配任何包含test的内容。
  
## 二、基础命令
  
### ls命令
- 语法：ls [-a -l -h] [路径]  

-a -l-h 是可选的选项,Linux路径是此命令可选的参数,当不使用选项和参数，直接使用ls命令本体，表示：以平铺形式，列出当前工作目录下的内容,
-a:把隐藏文件展示出来,-l:以列表的形式展现出来,-h:和-l一起使用显示文件大小单位  
  
### cd命令：切换工作目录
- 语法：cd [Linux路径]  

cd命令无需选项，只有参数，表示要切换到哪个目录下，cd命令直接执行，不写参数，表示回到用户的HOME目录。
  
### pwd命令
- 语法：pwd
查看当前所在工作目录。
  
### mkdir命令：创建新的目录
- 语法：mkdir [-p] Linux路径  

参数必填，表示Linux路径，即要创建的文件夹的路径，相对路径或绝对路径均可，-p选项可选，表示自动创建不存在的父目录，适用于创建连续多层级的目录
注意：创建文件夹需要修改权限，请确保操作均在HOME目录内，不要在HOME外操作涉及到权限问题，HOME外无法成功后续我们会讲解权限管控的知识。
  
### touch命令：创建文件
- 语法：touch Linux路径  

touch命令无选项，参数必填，表示要创建的文件路径，相对、绝对、特殊路径符均可以使用。  
  
### cat命令：查看文件内容
- 语法：cat 文件路径  
  
### more命令：查看文件内容
- 语法：more Linux路径  

more命令同样可以查看文件内容，同cat不同的是more是像翻书一样一页一页展示，按空格可以翻页，按q可以退出。
  
### cp命令：复制文件文件夹
- 语法：cp [-r]参数1 参数2  

-r选项，可选，用于复制文件夹使用，表示递归，参数1：Linux路径表示被复制的文件或文件夹，参数2：Linux路径表示要复制去的地方。
    
### mv命令：移动文件或文件夹
- 语法：mv 参数1 参数2    

参数1：Linux路径表示被移动的文件或文件夹，参数2：Linux路径表示要移动去的地方，如果目标不存在，则进行改名，确保目标存在（重命名）。  

### rm命令：删除文件、文件夹
- 语法：rm [-r -f]参数1 参数2 ...... 参数N    

同cp命令一样，-r选项用于删除文件夹，-f表示force，强制删除（不会弹出提示确认信息），普通用户删除内容不会弹出提示，只有root管理员用户删除内容会有提示•所以一般普通用户用不到-f选项，
参数1、参数2、.……….、参数N表示要删除的文件或文件夹路径，按照空格隔开。
     
### find命令-：按文件名查找文件和按文件大小查找文件
- 语法：find 起始路径 -name "被查找文件名"
- 语法：find 起始路径 -size +l-n[kMG]  

+、- 表示大于和小于，n表示大小数字，kMG表示大小单位，k(小写字母)表示kb，M表示MB，G表示GB，为了确保后续演示，拥有最大的权限，可以在整个系统完成搜索。。

# 管道符|
- 将|左边的输出作为|右边的输入  

### grep命令
- 语法：grep [-n]关键字 文件路径(要是能显示出来的文本，使用ls 命令显示的文本也可以作为参数 用管道符|)  

选项-n，可选，表示在结果中显示匹配的行的行号。参数：关键字必填，表示过滤的关键字，带有空格或其它特殊符号，建议使用””将关键字包围起来，参数：文件路径必填，表示要过滤内容的文件路径，可作为内容输入端口。   
  
### wc命令做数量统计
- 可以通过wc命令统计文件的行数、单词数量等
- 语法：wc [-c -m -1 -w] 文件路径
- 选项：-c，统计bytes数量
- 选项：-m，统计字符数量
- 选项：-l，统计行数
- 选项：-W，统计单词数量
- 参数：文件路径，被统计的文件，可作为内容输入端口   
  
### echo命令
- 可以使用echo命令在命令行内输出指定内容
- 语法：echo 输出的内容
- 无需选项，只有一个参数，表示要输出的内容，复杂内容可以用""包围 。
- "``"的优先级很高，被""包围也能使用
![img_1.png](Image/computer/Linux/Linux_image1.png)   
  
### 重定向符
- 我们再来学习两个特殊符号，重定向符：>和>>。
-  ">"：将左侧命令的结果，覆盖写入到符号右侧指定的文件中。
-  ">>"：将左侧命令的结果，追加写入到符号右侧指定的文件中。
  
## vim编辑器

### 打开vim编辑器
- 语法：vim 文件路径  
  
如果文件路径表示的文件不存在，那么此命令会用于编辑新文件，如果文件路径表示的文件存在，那么此命令用于编辑已有文件。
  
### vim编辑器的三种工作方式
![img_3.png](Image/computer/Linux/Linux_image3.png)
- 命令模式（Command mode）：我打开vim时自动处于命令模式，所敲的按键编辑器都理解为命令，以命令驱动执行不同的功能。此模型下，不能自由进行文本编辑，可以用于移动游标查看内容。
- 命令模式下的指令：
  ![img_2.png](Image/computer/Linux/Linux_image2.png) 
- 输入模式（Insert mode）：也就是所谓的编辑模式、插入模式，在命令模式按下i键进入，在该模式下按下ESC键回到命令模式，此模式下，可以对文件内容进行自由编辑。
- 底线命令模式(Last line mode）：在命令模式下按下":"进入。
- 底线模式下的指令：
  ![img_4.png](Image/computer/Linux/Linux_image4.png)
  
## 进程管理